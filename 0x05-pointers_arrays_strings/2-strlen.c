#include "main.h"

/**
 * _strlen - a function that returns the length of a string.
 *
 * @s: take a string
 *
 * Return: length of a characters
*/

int _strlen(char *s)
{

	int length = 0;


	while (*s != '\0')
	{

		length++
	}
	return (length);
}
